# claude-mem does not require any API keys or environment variables.
# It runs entirely on your local machine and stores data under ~/.claude-mem/.
#
# This file is intentionally empty — it exists so the onboarding flow knows
# claude-mem has no env requirements.
